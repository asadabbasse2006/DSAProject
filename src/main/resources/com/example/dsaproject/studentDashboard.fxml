<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.dsaproject.Controllers.adminDashboardController"
            stylesheets="@styles.css">

    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" styleClass="dashboard-header">
            <padding>
                <Insets top="15" right="20" bottom="15" left="20"/>
            </padding>
            <Label text="Student Dashboard" styleClass="dashboard-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="welcomeLabel" styleClass="welcome-label"/>
            <Button text="Logout" onAction="#handleLogout" styleClass="secondary-button"/>
        </HBox>
    </top>

    <left>
        <VBox spacing="10" styleClass="sidebar">
            <padding>
                <Insets top="20" right="15" bottom="20" left="15"/>
            </padding>

            <Button text="🏠 Home" onAction="#showRoutes" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="🗺️ View Routes" onAction="#showRoutes" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="🎫 Book Seat" onAction="#showRoutes" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="📋 My Bookings" onAction="#showRoutes" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="⏰ Bus Schedule" onAction="#showRoutes" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Region VBox.vgrow="ALWAYS"/>
            <Label text="Quick Stats" styleClass="sidebar-section-title"/>
            <VBox spacing="5" styleClass="stats-box">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>
                <Label fx:id="activeBookingsLabel" text="Active: 0" styleClass="stat-label"/>
                <Label fx:id="availableRoutesLabel" text="Routes: 0" styleClass="stat-label"/>
            </VBox>
        </VBox>
    </left>

    <center>
        <StackPane fx:id="contentArea" styleClass="content-area">
            <!-- Dynamic content loaded here -->
        </StackPane>
    </center>
</BorderPane>