<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.example.dsaproject.Controllers.adminDashboardController"
            stylesheets="@styles.css">

    <top>
        <HBox alignment="CENTER_LEFT" spacing="20" styleClass="dashboard-header">
            <padding>
                <Insets top="15" right="20" bottom="15" left="20"/>
            </padding>
            <Label text="Admin Dashboard" styleClass="dashboard-title"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="welcomeLabel" styleClass="welcome-label"/>
            <Button text="Logout" onAction="#handleLogout" styleClass="secondary-button"/>
        </HBox>
    </top>

    <left>
        <VBox spacing="10" styleClass="sidebar">
            <padding>
                <Insets top="20" right="15" bottom="20" left="15"/>
            </padding>

            <Button text="📊 Dashboard" onAction="#showDashboard" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="🗺️ Manage Routes" onAction="#showRoutes" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="🚌 Manage Buses" onAction="#showBuses" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="👤 Manage Drivers" onAction="#showDrivers" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="📋 View Bookings" onAction="#showBookings" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Button text="📈 Reports" onAction="#showReports" styleClass="sidebar-button" maxWidth="Infinity"/>
            <Separator/>
            <Button text="⏳ Waiting List" onAction="#showWaitingList" styleClass="sidebar-button" maxWidth="Infinity"/>
        </VBox>
    </left>

    <center>
        <StackPane fx:id="contentArea" styleClass="content-area">
            <!-- Dynamic content loaded here -->
        </StackPane>
    </center>
</BorderPane>